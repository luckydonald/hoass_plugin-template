- id: no-co-authored-by
  name: Reject Co-Authored-By in commit messages
  entry: uv run --no-sync python scripts/°base/git/hooks/commit/reject_co_authored_by.py
  language: system
  stages: [commit-msg]
  always_run: true
